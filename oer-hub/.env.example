# Optional: Gemini API key for AI features (Block B/C). Without it, AI calls show an error.
VITE_GEMINI_API_KEY=

# Set to "true" to require onboarding before accessing dashboards
VITE_ENABLE_ONBOARDING=false
